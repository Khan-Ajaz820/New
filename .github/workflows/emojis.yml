name: 🧩 Build SVG Sprite

on:
  push:
    branches: [ main ]   # or "master" — update if needed
  workflow_dispatch:      # allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your repo
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: 🧱 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'   # Node 20+ recommended

      # 3️⃣ Install dependencies (if you have any)
      - name: 📦 Install dependencies
        run: npm install

      # 4️⃣ Run your sprite builder
      - name: 🧩 Build SVG Sprite
        run: npm run build

      # 5️⃣ Upload sprite as artifact (for download)
      - name: 📤 Upload sprite.svg artifact
        uses: actions/upload-artifact@v4
        with:
          name: sprite-svg
          path: sprite.svg

      
